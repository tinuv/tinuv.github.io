---
layout: post
title: '小结'
subtitle: ''
date: 2019-2-21
categories: PAT C++
cover: ''
tags: PAT C++
---



# 结构体
结构体以前的时候用过,但现在不记得了,回忆一下
## 创建一个结构体
```cpp
struct t { // 结构体关键字 结构体名
    string id;
    float timein;
    float timeout;
} people[1000];// 变量名
```

## 结构体输入
通过点号就行了
```cpp
cin >> people[i].id;
```

# 类型转换
主要是整型,浮点与字符串互转

## 字符转整形
就是单个字符转化为整形,不知道别人是怎么搞得,我反正是这样搞得
```cpp
      for(int i = 0; i < origin.size(); i++) {
        char a[1];
        a[0] = origin[i];
        result = result + stoi(a);
        }
```


## 字符串转整型
使用atoi()函数,因为atoi是c标准库的函数,而string是c++的stl,所以直接转不行,需要使用string.c_str()这个函数转化为c类型的字符串,也就是字符数组
```cpp
for(int i = 0; i < rn; i++) {
        cin >> people[i].id;
        string signin, signout;
        cin >> signin >> signout;
        string secondin = signin.substr(6, 2);
        string minin = signin.substr(3, 2);
        string hourin = signin.substr(0, 2);
        float timein = ((atoi(signin.substr(6, 2).c_str()) / 60.0) + atoi(signin.substr(3, 2).c_str())) / 60.0 + atoi(signin.substr(0, 2).c_str());
        float timeout = ((atoi(signout.substr(6, 2).c_str()) / 60.0) + atoi(signout.substr(3, 2).c_str())) / 60.0 + atoi(signout.substr(0, 2).c_str());
        people[i].timein = timein;
        people[i].timeout = timeout;
    }
```

## 其他类型转字符串
这个很简单,to_string()函数可以解决大部分问题


# map类初始化

```cpp

map<char, string> m = {{'0', "zero"}}, {'1', "one"}, {'2', "two"}, {'3', "three"}, {'4', "four"}, {'5', "five"}, {'6', "six"}, {'7', "seven"}, {'8', "eight"},{'9', "nine"}};

```

与java语言有点不同

# sort()函数
如果不是基本类型,最好将元素封装成结构体,配合lambda表达式美滋滋
```cpp
struct t {
    string id;
    float timein;
    float timeout;
} people[1000];

sort(people, people + rn, [](t a, t b)->bool {
        return a.timein < b.timein;
    });
```